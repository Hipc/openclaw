{
  "id": "stt",
  "name": "STT Plugin",
  "description": "Speech-to-Text gateway plugin. Transcribes base64 audio via a named STT provider config and forwards the result to the specified session.",
  "configSchema": {
    "type": "object",
    "additionalProperties": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "apikey"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["openai", "aliyun"],
          "description": "STT provider type. Determines the API request format. Supported: openai, aliyun."
        },
        "apikey": {
          "type": "string",
          "description": "API key for the STT provider."
        },
        "url": {
          "type": "string",
          "description": "Optional custom STT endpoint URL. Defaults per type: openai uses OpenAI Whisper API, aliyun uses DashScope chat completions."
        },
        "model": {
          "type": "string",
          "description": "Model name to use. Defaults to whisper-1 for openai and qwen-asr-flash for aliyun."
        }
      }
    },
    "description": "Map of named STT configs. Each key is a config name; value contains apikey and an optional url override."
  }
}
